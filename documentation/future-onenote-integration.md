# Future OneNote Integration

OneNote ingestion should use delegated Microsoft authorization through Microsoft Graph. It should convert OneNote pages into the existing recipe-candidate and review pipeline rather than adding a separate recipe model.

OneNote auth is not required for the MVP.

